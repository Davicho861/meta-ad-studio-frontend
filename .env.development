# Development environment variables for full-stack local development
# Do NOT commit secrets for production. These are safe defaults for local dev only.

# Backend
NODE_ENV=development
PORT=8000

# Database (Postgres)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Frontend -> Backend (inside Docker network use service name 'backend')
# Vite will expose this as import.meta.env.VITE_API_BASE_URL
VITE_API_BASE_URL=http://backend:8000
