#!/usr/bin/env bash

# Run test suite before pushing
echo "Running frontend tests (pre-push) in meta-ad-studio..."
cd meta-ad-studio || exit 0
npm test || {
	echo "Frontend tests failed. Push aborted." >&2
	exit 1
}

