import React from 'react'
import Layout from './components/Layout'

// MainUI now simply renders the new Layout component to ensure the app
// boots into the Generative Ad Studio UI.
export default function MainUI() {
  return <Layout />
}
